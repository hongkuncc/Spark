package com.hongkuncc.offline.bean

case class CategorySession(taskId: String,
                           categoryId: String,
                           sessionId: String,
                           clickCount: Long)

